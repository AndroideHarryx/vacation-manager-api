'use strict';

/**
 *  awaiting-request controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::awaiting-request.awaiting-request');
